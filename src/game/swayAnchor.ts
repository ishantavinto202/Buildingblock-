/** @deprecated Import from `./coordinates` — re-exported for existing imports. */
export {
  getGlobalSwayAnchorCx,
  getHorizontalMovementBounds,
  getMaxSwayAmplitudePx,
  type HorizontalMovementBounds,
} from './coordinates';
